[pytest]
asyncio_mode = auto
markers =
    domain: deterministic commerce-domain and transaction invariants
    contract: runtime and tool-surface contract checks
    transcript: scripted model transcript evaluations through the real runtime loop
testpaths = tests
pythonpath = .
